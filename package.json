{
  "name": "tembiter",
  "version": "0.0.1-alpha.2",
  "private": false,
  "description": "Arbiter for template format, setup CLI, and skills.",
  "type": "module",
  "bin": {
    "tembiter": "./dist/src/cli.js"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "test": "tsc && node --test 'dist/test/**/*.test.js'"
  },
  "files": [
    "dist/src"
  ],
  "devDependencies": {
    "@types/node": "^20.19.11",
    "typescript": "^5.9.2"
  }
}
